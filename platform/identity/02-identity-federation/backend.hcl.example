# Copy to backend.hcl (gitignored) and run: terraform init -backend-config=backend.hcl

resource_group_name  = "rg-myorg-platform-bootstrap"
storage_account_name = "stmystatename"
container_name       = "tfstate"
use_azuread_auth     = true

tenant_id       = "00000000-0000-0000-0000-000000000000"
subscription_id = "00000000-0000-0000-0000-000000000000"

key = "platform/identity/02-identity-federation.tfstate"
