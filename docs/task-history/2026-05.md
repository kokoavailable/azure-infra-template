# 2026-05 Task History

## 2026-05-10 - Restart-Safe Codex Workflow

Status:

- Materially advanced

Changed:

- Added `.codex/session-notes/current.md` for restart context.
- Added `.codex/session-notes/archive/.gitkeep` for archived session notes.
- Updated `AGENTS.md` and `docs/codex-workflow.md` so non-trivial tasks keep
  session notes.

Validation:

- Targeted Prettier checks passed for changed workflow and session note files.

Follow-up:

- Keep session notes current during non-trivial work.
- Archive completed notes when useful.

Related:

- `.codex/session-notes/current.md`
- `docs/codex-workflow.md`

## 2026-05-10 - Troubleshooting Notes System

Status:

- Materially advanced

Changed:

- Added `docs/troubleshooting/README.md`.
- Added `docs/troubleshooting/index.md`.
- Added `docs/troubleshooting/template.md`.
- Added first troubleshooting note for the `make docs-fmt` `EPERM` failure.
- Updated `AGENTS.md`, `docs/codex-workflow.md`, and `docs/runbook.md` so
  recurring failures and recovery procedures become durable notes.

Validation:

- Targeted Prettier checks passed for changed troubleshooting and workflow
  files.

Follow-up:

- Fix or intentionally scope `make docs-fmt` so repository-wide documentation
  formatting is reliable.

Related:

- `docs/troubleshooting/2026-05-10-docs-fmt-eperm.md`
- `docs/troubleshooting/index.md`
- `docs/runbook.md`

## 2026-05-10 - Learning and Agent Prompt System

Status:

- Materially advanced

Changed:

- Added `docs/learning/inbox.md` for concepts the user actually struggled with.
- Added `.codex/prompts/diff-review.md`.
- Added `.codex/prompts/session-postmortem.md`.
- Added `.codex/prompts/knowledge-compiler.md`.
- Updated `docs/codex-workflow.md` and `docs/ai-native-workflow.md` to separate
  runbook, troubleshooting, learning, Codex workflow, and session notes.

Validation:

- Targeted Prettier checks passed for changed learning, prompt, and workflow
  files.

Follow-up:

- Use learning entries sparingly, only for concepts that actually blocked or
  confused the user.

Related:

- `docs/learning/inbox.md`
- `.codex/prompts/diff-review.md`
- `.codex/prompts/session-postmortem.md`
- `.codex/prompts/knowledge-compiler.md`

## 2026-05-10 - Project Roadmap

Status:

- Materially advanced

Changed:

- Added `docs/roadmap.md` with eight project phases, tasks, done criteria, and
  validation gates.
- Linked `docs/roadmap.md` from `AGENTS.md`.

Validation:

- `npx prettier --check AGENTS.md docs/roadmap.md .codex/session-notes/current.md`
  passed.

Follow-up:

- Start the next roadmap task: fix or intentionally scope the `make docs-fmt`
  workflow.

Related:

- `docs/roadmap.md`
- `docs/troubleshooting/2026-05-10-docs-fmt-eperm.md`

## 2026-05-10 - Platform Management README Handoff

Status:

- Completed

Changed:

- Updated `platform/management/03-policy-governance/README.md` to use Makefile
  commands instead of raw `terraform` commands.
- Added platform change requirement details for affected path, affected Azure
  scope, blast radius, validation command, rollback note, and remaining risk.
- Clarified that plan needs expected Azure/backend access and apply requires
  explicit approval.

Validation:

- `npx prettier --check platform/management/03-policy-governance/README.md`
  passed.
- `make validate STACK=platform/management/03-policy-governance` passed with
  provider registry access.

Follow-up:

- Move to `platform/identity/02-identity-federation` OIDC/RBAC review.

Related:

- `platform/management/03-policy-governance/README.md`
- `platform/AGENTS.md`

## 2026-05-10 - Platform Identity README Handoff

Status:

- Completed

Changed:

- Updated `platform/identity/02-identity-federation/README.md` to use Makefile
  commands instead of raw `terraform` commands.
- Added platform change requirement details for Entra application/service
  principal, GitHub federated credentials, subscription RBAC, and optional
  tfstate Blob RBAC.
- Clarified that plan needs expected Azure/backend access and apply requires
  explicit approval.

Validation:

- `npx prettier --check platform/identity/02-identity-federation/README.md`
  passed.
- `make validate STACK=platform/identity/02-identity-federation` passed with
  provider registry access.

Follow-up:

- Review OIDC branch subjects, audience assumptions, and RBAC scope before any
  plan/apply.
- Continue platform validation with connectivity or shared-services scaffold
  review.

Related:

- `platform/identity/02-identity-federation/README.md`
- `platform/AGENTS.md`

## 2026-05-10 - Executable Agent Workflow Targets

Status:

- Completed

Changed:

- Added `scripts/agent/render-prompt.sh` to render prompt-plus-context bundles.
- Added Makefile targets:
  - `make agent-diff-review`
  - `make agent-session-postmortem`
  - `make agent-knowledge-compile`
- Documented Makefile agent workflows in `docs/codex-workflow.md`,
  `docs/ai-native-workflow.md`, and `docs/roadmap.md`.

Validation:

- `bash -n scripts/agent/render-prompt.sh` passed.
- `make help` showed the new `agent-*` targets.
- `make agent-diff-review AGENT_OUT=/private/tmp/agent-diff-review.md`
  generated a bundle.
- `make agent-session-postmortem AGENT_OUT=/private/tmp/agent-session-postmortem.md`
  generated a bundle.
- `make agent-knowledge-compile AGENT_OUT=/private/tmp/agent-knowledge-compile.md`
  generated a bundle.

Follow-up:

- Use `make agent-diff-review` before reviewing larger diffs.
- Use `make agent-session-postmortem` when a session exposes a recurring
  failure or reusable recovery pattern.
- Use `make agent-knowledge-compile` at the end of a work day.

Related:

- `Makefile`
- `scripts/agent/render-prompt.sh`
- `.codex/prompts/diff-review.md`
- `.codex/prompts/session-postmortem.md`
- `.codex/prompts/knowledge-compiler.md`

## 2026-05-10 - Operating Loop Agent Entrypoint

Status:

- Completed

Changed:

- Added `.codex/prompts/operating-loop.md`.
- Added `make agent-operating-loop`.
- Added `TASK=<text>` support for agent workflow bundles.
- Updated workflow docs so ambiguous work starts by separating automatable work
  from human judgment gates.

Validation:

- `bash -n scripts/agent/render-prompt.sh` passed.
- `make help` showed `agent-operating-loop`.
- `make agent-operating-loop TASK='decide the next platform task safely' AGENT_OUT=/private/tmp/agent-operating-loop.md`
  generated a bundle.
- `npx prettier --check .codex/prompts/operating-loop.md docs/codex-workflow.md docs/ai-native-workflow.md docs/roadmap.md docs/task-history/2026-05.md .codex/session-notes/current.md`
  passed.

Follow-up:

- Use `make agent-operating-loop TASK="..."` before broad, ambiguous, or
  high-risk work.

Related:

- `.codex/prompts/operating-loop.md`
- `Makefile`
- `scripts/agent/render-prompt.sh`

## 2026-05-10 - Platform Public DNS README Handoff

Status:

- Completed

Changed:

- Updated `platform/connectivity/global/00-dns-public/README.md` to use
  Makefile commands instead of raw `terraform` commands.
- Added platform change requirement details for public DNS zone, optional TXT
  records, blast radius, rollback, validation, and remaining risk.
- Documented registrar NS delegation as a human gate outside Azure state.
- Added `.codex/agent-runs/` to `.gitignore` because generated bundles may
  include local diffs and task context.

Validation:

- `make validate STACK=platform/connectivity/global/00-dns-public` passed with
  provider registry access.
- `npx prettier --write platform/connectivity/global/00-dns-public/README.md`
  completed.

Follow-up:

- Review the public DNS stack before any plan/apply.
- Do not change registrar NS delegation without explicit human approval and a
  rollback note.

Related:

- `platform/connectivity/global/00-dns-public/README.md`
- `.gitignore`
- `platform/AGENTS.md`

## 2026-05-10 - Agent Operating Guide

Status:

- Completed

Changed:

- Added `docs/agent-operating-guide.md` as the operator-facing guide for
  running project work through agent workflow commands.
- Linked `docs/agent-operating-guide.md` from `AGENTS.md`.

Validation:

- `npx prettier --check docs/agent-operating-guide.md AGENTS.md docs/task-history/2026-05.md`
  passed.

Follow-up:

- Use the guide when starting new non-trivial tasks.
- Keep the guide aligned when Makefile `agent-*` targets change.

Related:

- `docs/agent-operating-guide.md`
- `Makefile`
- `.codex/prompts/operating-loop.md`

## 2026-05-10 - Platform Hub Network Scaffold Handoff

Status:

- Completed

Changed:

- Expanded `platform/connectivity/kr/koreacentral/hub/00-hub-network/README.md`
  from a one-line scaffold into a platform handoff document.
- Documented intended scope, out-of-scope boundaries, downstream dependency
  direction, human decision gates, platform change requirements, validation gap,
  and implementation notes.

Validation:

- `npx prettier --write platform/connectivity/kr/koreacentral/hub/00-hub-network/README.md`
  completed.
- Stack validation is not applicable yet because this scaffold has no `.tf`
  files.

Follow-up:

- Review CIDR, subnet, peering, route, firewall, Bastion, and production
  connectivity assumptions before adding OpenTofu resources.
- Continue platform connectivity handoff with `05-private-dns`.

Related:

- `platform/connectivity/kr/koreacentral/hub/00-hub-network/README.md`
- `platform/AGENTS.md`

## 2026-05-10 - Platform Private DNS Scaffold Handoff

Status:

- Completed

Changed:

- Expanded `platform/connectivity/kr/koreacentral/hub/05-private-dns/README.md`
  from a one-line scaffold into a platform handoff document.
- Documented intended scope, out-of-scope boundaries, downstream dependency
  direction, human decision gates, platform change requirements, validation gap,
  and implementation notes.

Validation:

- `npx prettier --write platform/connectivity/kr/koreacentral/hub/05-private-dns/README.md`
  completed.
- Stack validation is not applicable yet because this scaffold has no `.tf`
  files.

Follow-up:

- Review centralized zone list, VNet link registration policy, hybrid resolver
  assumptions, production resolution, and migration from duplicate zones before
  adding OpenTofu resources.
- Continue platform connectivity handoff with `10-egress-routing-security`.

Related:

- `platform/connectivity/kr/koreacentral/hub/05-private-dns/README.md`
- `docs/dns-strategy.md`
- `platform/AGENTS.md`

## 2026-05-10 - Platform Egress Routing Security Scaffold Handoff

Status:

- Completed

Changed:

- Expanded `platform/connectivity/kr/koreacentral/hub/10-egress-routing-security/README.md`
  from a one-line scaffold into a platform handoff document.
- Documented intended scope, out-of-scope boundaries, downstream dependency
  direction, human decision gates, platform change requirements, validation gap,
  and implementation notes.

Validation:

- `npx prettier --write platform/connectivity/kr/koreacentral/hub/10-egress-routing-security/README.md`
  completed.
- Stack validation is not applicable yet because this scaffold has no `.tf`
  files.

Follow-up:

- Review forced-tunnel design, default route propagation, firewall policy, NAT
  ownership, route association model, production egress behavior, and broad
  allow rules before adding OpenTofu resources.
- Continue platform connectivity handoff with `20-access-ops`.

Related:

- `platform/connectivity/kr/koreacentral/hub/10-egress-routing-security/README.md`
- `docs/security-baseline.md`
- `platform/AGENTS.md`

## 2026-05-10 - Platform Hub Network OpenTofu Scaffold

Status:

- Materially advanced

Changed:

- Added the first real OpenTofu scaffold for
  `platform/connectivity/kr/koreacentral/hub/00-hub-network`.
- Added backend, provider, version, locals, variables, main resources, outputs,
  backend example, tfvars example, and provider lock file.
- Implemented resource group, hub virtual network, and subnet map resources.
- Kept hub CIDR and subnet layout as explicit input variables so architecture
  decisions remain human approval gates.
- Updated the hub network README with Makefile validation and plan commands.

Validation:

- `make fmt` passed.
- `make validate STACK=platform/connectivity/kr/koreacentral/hub/00-hub-network`
  first failed inside the sandbox because provider registry DNS was blocked.
- `make validate STACK=platform/connectivity/kr/koreacentral/hub/00-hub-network`
  passed with provider registry access.

Follow-up:

- Review and approve `hub_address_space` and `hub_subnets` before plan/apply.
- After CIDR and subnet approval, run
  `make plan STACK=platform/connectivity/kr/koreacentral/hub/00-hub-network`.
- Continue with the next real platform implementation task only after reviewing
  the hub network plan.

Related:

- `platform/connectivity/kr/koreacentral/hub/00-hub-network/main.tf`
- `platform/connectivity/kr/koreacentral/hub/00-hub-network/variables.tf`
- `platform/connectivity/kr/koreacentral/hub/00-hub-network/outputs.tf`
- `platform/connectivity/kr/koreacentral/hub/00-hub-network/README.md`
- `platform/AGENTS.md`

## 2026-05-10 - Agent Workflow Closure After Hub Scaffold

Status:

- Completed

Changed:

- Generated diff review and session postmortem bundles after the real hub
  network scaffold implementation.
- Added a troubleshooting note for agent workflow drift into documentation-only
  loops.
- Added a learning inbox entry for the difference between context bundles and
  implementation loops.

Validation:

- `make agent-diff-review AGENT_OUT=.codex/agent-runs/diff-review.md` generated
  a review bundle after elevated file access was required for the ignored
  `.codex/agent-runs/` output path.
- `make agent-session-postmortem AGENT_OUT=.codex/agent-runs/session-postmortem.md`
  generated a postmortem bundle after elevated file access was required for the
  ignored `.codex/agent-runs/` output path.

Follow-up:

- Keep `agent-*` bundle generation after implementation for normal tasks, not
  before every small edit.
- Use troubleshooting and learning updates only when the session exposes a
  reusable failure mode or concept.

Related:

- `docs/troubleshooting/2026-05-10-agent-workflow-documentation-loop.md`
- `docs/learning/inbox.md`
- `.codex/agent-runs/diff-review.md`
- `.codex/agent-runs/session-postmortem.md`
